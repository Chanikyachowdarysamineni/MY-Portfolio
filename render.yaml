services:
  - type: web
    name: portfolio-backend
    env: node
    plan: free
    rootDir: server
    startCommand: npm start
    # Using Procfile + package.json for deployment
    # Build process: npm install (root) -> npm run build (installs server deps) -> npm start (runs backend)
    envVars:
      - key: PORT
        value: 10000
      - key: MONGO_URI
        value: mongodb+srv://raavanaasura87_db_user:Chani8877@chani.irvyksk.mongodb.net/portfolio?retryWrites=true&w=majority
      - key: NODE_ENV
        value: production
